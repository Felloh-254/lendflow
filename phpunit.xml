<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>

        <!--
            We deliberately test against real Postgres, not SQLite.
            Row-level locking (lockForUpdate), the concurrency test suite,
            and PostgreSQL-specific constraints (check constraints, unique
            constraints) are core to what this project demonstrates —
            SQLite's locking model is different enough that it would let
            broken concurrency code pass the test suite.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="postgres"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="lendflow_testing"/>
        <env name="DB_USERNAME" value="lendflow"/>
        <env name="DB_PASSWORD" value="secret"/>

        <env name="JWT_TTL" value="15"/>
        <env name="JWT_REFRESH_TTL" value="20160"/>
    </php>
</phpunit>
